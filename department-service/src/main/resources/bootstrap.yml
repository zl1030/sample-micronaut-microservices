micronaut:
  application:
    name: department-service
    instance:
      id: ${random.shortuuid}
  config-client:
    enabled: true

consul:
  client:
    defaultZone: "localhost:8500"
    config:
      format: YAML